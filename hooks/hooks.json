{
  "description": "Shell auto-formatting on edit. The session-start collectors now live in the plugin that owns each one — see plugins/*/hooks/.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/post-file-edit.sh ${CLAUDE_PLUGIN_ROOT}",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
